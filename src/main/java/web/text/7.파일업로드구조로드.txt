- 스트림 : 데이터가 다니는 통로의 흐름
    -주로 자바 외 장치와 데이터 주고 받을 때 사용 : 키보드 , 파일처리 , JDBC처리 , 네트워크

- HTML 에서 첨부파일 입력받을 때
    1. <input type="file" />
    2. JS에서 첨부파일의 Value 호출하면 : document.querySelector('.bfile').value
    - "C:\fakepath\업로드한 파일명.파일타입"
    - JS는 유저(클라이언트)의 실제 파일 경로를 얻어낼수 없다. (브라우저 규칙/법/규정)
- 업로드 : 파일 올리기
    - 유저(클라이언트)  PC/모바일 의 파일을 서버(스프링 내장 톰캣 서버 ) 로 이동/복사
    - 이동/복사
        - 대용량 이동시에는 HTTP 는 바이트형식 만 지원
        -

- 다운로드 : 파일 내려받기

1. 업로드 구현 방법.
    1. HTML : <input type="file" class="bfile"/>
    2. HTML : <form class="boardWriteForm"> </form>// 폼에 이름설정
    •FORM에 전체 HTML 호출

    3. JS : let boardWriteForm = document.querySelector('.boardWriteForm').
    •from 전체 HTML을 바이트로 변환해주는 기능이 담긴 객체로 변환
        - new FormData()
        JS :  let boardWriteFormData = new FormData(boardWriteForm);

    4. AJAX 대용량 전송 필수 옵션
        - method : post , put
        - data : formData
        - contentType : false
        - processData : false
* HTML FORM 과 JAVA의 boardDto 필드와 매핑 할때 주의할점
   -HTML 속성과 dto의 필드명 과 동일해야한다.

2. 자바에서 대용량 바이트의 타입
    - MultipartFile 인터페이스 : Spring 에서 제공하는 라이브러리
        - 주로 대용량 바이트 파일을 조작할때 사용되는 인터페이스 (결론 : 첨부파일을 바이트형변환 하고 MultipartFile 로 바이트 파일 조작)